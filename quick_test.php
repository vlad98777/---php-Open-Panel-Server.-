<?php
echo "<h3>Проверка MySQL</h3>";

// Простой тест подключения
try {
    $pdo = new PDO("mysql:host=127.0.0.1;port=3306", "root", "");
    echo "✅ MySQL запущен и доступен!<br>";
    
    // Проверка базы
    $databases = $pdo->query("SHOW DATABASES")->fetchAll(PDO::FETCH_COLUMN);
    echo "📁 Базы данных: " . implode(', ', $databases) . "<br>";
    
} catch (Exception $e) {
    echo "❌ MySQL НЕ запущен: " . $e->getMessage() . "<br>";
    echo "<h4>Действия:</h4>";
    echo "1. Запустите MySQL через панель управления (XAMPP/OpenServer)<br>";
    echo "2. Убедитесь что порт 3306 свободен<br>";
    echo "3. Проверьте что нет других запущенных MySQL серверов<br>";
}
?>